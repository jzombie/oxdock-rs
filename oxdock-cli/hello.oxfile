# TODO: Show basic examples of "hello world" usage from various OS platforms

[platform=mac] RUN echo "Hello from Mac"

[platform=linux] RUN echo "Hello from Linux"

[platform=windows] RUN echo "Hello from Windows"

[env:FOO] RUN echo "Foo is set?"

ENV FOO=1

[env:FOO] RUN echo "Foo is now set..."

# RUN env | grep FOO
