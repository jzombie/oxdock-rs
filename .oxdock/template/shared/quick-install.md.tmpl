Add it to your Rust build with `cargo add oxdock@{{ env:CRATE_VERSION }}`, or install the standalone runner with `cargo install oxdock@{{ env:CRATE_VERSION }}`.

Run a script:

```sh
oxdock <PATH>
```
