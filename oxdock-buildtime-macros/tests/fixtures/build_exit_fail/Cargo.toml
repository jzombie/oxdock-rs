[package]
name = "fixture-build-exit-fail"
version = "0.1.0-alpha"
edition = "2024"
publish = false
license = "Apache-2.0"

[workspace]

[dependencies]
oxdock-buildtime-macros = { path = "../../.." }
rust-embed = { version = "8.9.0", features = ["include-exclude"] }
