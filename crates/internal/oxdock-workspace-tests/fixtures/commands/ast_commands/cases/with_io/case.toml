name = "with_io"
stdin = "hello from stdin"

[expect]
files = { "empty.txt" = "" }

[expect.platform.unix]
files = { "out.txt" = "hello from stdin" }

[expect.platform.windows]
files = { "out.txt" = "hello from stdin" }
