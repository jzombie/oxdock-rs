# Plan: Sharpen README taglines + version-pinned install without pushing Quick start below fold

## Context (verified on disk)
- READMEs are generated by `crates/docs-gen` (`cargo run -p docs-gen`). Do NOT edit `README.md` directly.
- User's `crates/oxdock/README.md` ref was the deleted placeholder crate (1-line "reserving the oxdock name"). Real front-door is `oxdock/README.md`, whose line 3 (`Run OxDock scripts from the CLI, or embed them in your build.`) comes from `oxdock/Cargo.toml:3 description` via `oxdock/.oxdock/template/header.tmpl` (`{{ $docs_ctx.description }}`).
- Root tagline `OxDock is a Dockerfile-inspired DSL for build-time work. No containers, no daemon.` comes from `.oxdock/template/readme/fragments/intro.md:1` (verbatim `read` stage via `.oxdock/template/readme/output.tmpl`).
- Root Quick start assembly: `fragments/quick-start.md` (`## Quick start` only) + `shared/embed-example.md` (rustc example) + `fragments/quick-start-cli.md` (oxdock example + `cargo install --path oxdock` at lines 23-28).
- Version-dynamic pattern: `oxdock/.oxdock/template/fragments/dependency.tmpl:11` uses `{{ env:CRATE_VERSION }}` (expanded `.tmpl`). `crates/docs-gen/src/main.rs:113-122` sets `CRATE_VERSION` env ONLY when `target.member` is `Some`. Root README target (`dir=.oxdock/template/readme`, `member=None` per `discovery.rs:member_for_dir`) gets NO `CRATE_VERSION` today — strict `EXPAND` would fail on unknown key.
- Workspace version: `Cargo.toml:27` = `0.9.0-alpha`. `providers.rs:118-130` already has `parse_workspace_version` fallback.

## User decisions
- Placeholder `crates/oxdock` deleted — ignore it, fix `oxdock/README.md` (Cargo description) + root `README.md`.
- Install style: `cargo install oxdock@{version}`, dynamic via same mechanism as `dependency.tmpl` (currently alpha, bare `cargo install oxdock` wouldn't resolve).
- Tagline: dual-mode — "a build DSL for Rust, or a generic pipeline generator that works as an executable binary".

## Changes

### 1. `oxdock/Cargo.toml:3` description (front-door selling point)
- Old: `Run OxDock scripts from the CLI, or embed them in your build.`
- New (proposal, no dash punctuation): `Dockerfile inspired build DSL. Native. Embed scripts at compile time in Rust, or run the same scripts as standalone CLI pipelines.`
- Effect: auto-flows into `oxdock/README.md:3` via existing `header.tmpl`; no template change needed there. Also improves crates.io listing.
- Verify: `cargo metadata` still parses; `cargo run -p docs-gen` regenerates `oxdock/README.md`.

### 2. `.oxdock/template/readme/fragments/intro.md` (root tagline)
- Old: `OxDock is a Dockerfile-inspired DSL for build-time work. No containers, no daemon.`
- New (compact, no dash punctuation, includes parity plus feature line):
  ```
  OxDock is a Dockerfile inspired build DSL for Rust. Embed scripts at compile time with macros, or run the same scripts as standalone CLI pipelines. Native. No containers. No daemon. No VM. All commands run identically on every OS, except RUN.

  Supports platform gating, async tasks, and piped workflows for custom pipelines.
  ```
- Note: tagline sentence stays fixed. Gating plus async plus pipes live in a second short paragraph in the same `intro.md` so the first line does not bloat. No dash punctuation in either paragraph.
- Both READMEs lead with the same copy via a single shared asset, no duplication: add `.oxdock/template/shared/intro.tmpl` once (must be `.tmpl`, not `.md`, so `{{ env:CRATE_VERSION }}` expands) with the approved two paragraph copy plus a version pinned docs link line, e.g. `[Documentation](https://docs.rs/oxdock/{{ env:CRATE_VERSION }}/oxdock/)`. Reference that exact file from both `.oxdock/template/readme/output.tmpl` and `oxdock/.oxdock/template/output.tmpl` via `{{> .oxdock/template/shared/intro.tmpl }}`. Retire root `fragments/intro.md` in favor of the shared include (remove it and update template). Sync `oxdock/Cargo.toml` description first sentence to match for crates.io. `overview.md` keeps the longer explainer below the lead.
- Requires the planned `CRATE_VERSION` fallback for member `None` targets in `crates/docs-gen/src/main.rs` (workspace version via existing `parse_workspace_version`), otherwise strict `EXPAND` fails on the root README. Verify rendered link reads `https://docs.rs/oxdock/0.9.0-alpha/oxdock/` today and rolls forward with the workspace version.
- Also rewrite `oxdock/src/lib.rs:1-14` crate docs (renders as docs.rs summary). Today the whole header is internal wiring notes: facade entry point, CLI runner lives in `oxdock-cli`, macros reexported unconditionally, command reference generated by `docs-gen`, fenced `oxdock` info strings for docs conformance. Replace all of that with the same shared intro copy used by both READMEs: what OxDock is, dual macro plus CLI use, Native, No containers, No daemon, No VM, parity except RUN, plus gating plus async plus pipes. No parser internals, no agent rules, no codegen notes. Keep `#![doc = include_str!("../docs/command_reference.md")]` and its `allow` silently (no explanatory prose) so the reference still renders. This file is hand edited, not generated, so sync it manually in the same commit.

### Mirror llama.cpp structure (quick, to the point, no fluff)
Reference: `https://github.com/ggml-org/llama.cpp` README pattern observed:
- Title plus logo, centered bold one line tagline (theirs: `LLM inference in C/C++`), compact badge row, one line link row.
- `## Quick start` immediately after header: short options list plus an `Once installed:` code block with two copy paste commands. No paragraphs before the code.
- `## Description` after quick start: goal sentence plus plain bullet list of capabilities.
- Long reference tables and contributor docs pushed lower under `Documentation` style sections.

Apply to both `README.md` and `oxdock/README.md` via the shared intro asset:
- Shared `.oxdock/template/shared/intro.tmpl`: bold one line tagline first (`Dockerfile inspired build DSL for Rust`), then the approved two paragraph lead plus docs.rs version pinned link. Keep it to tagline plus 3 short sentences max. Details (parity except RUN, gating, async, pipes) become a tight bullet list in Description style, not prose paragraphs.
- Root `output.tmpl` order: header badges, shared intro, `## Quick start` with install options (`cargo install oxdock@{{ env:CRATE_VERSION }}`, `cargo add`) plus `Once installed:` block (`oxdock --script Oxfile`, Rust `oxdock_embed!` pointer), then Description bullets, then existing DSL reference and coverage sections unchanged lower.
- `oxdock/output.tmpl`: same lead, same Quick start, then Common usage. No duplicate copy, both reference the shared intro file.
- Trim fluff in lead: remove registry versus path install explanations, codegen and conformance harness notes, and parser grammar pointers from the top. Those stay lower in `how-tested.md` and `dsl-reference.md` only.

### Cover prepare plus streaming pipes (READMEs lead with embed today, missing two modes)
- Add two short subsections after the embed example, shared by both READMEs via new shared fragments so both stay in sync:
  - `prepare` (`oxdock_prepare!`): same script shape as embed but emits no runtime module. One line why plus minimal Rust fence: `use oxdock_macros::oxdock_prepare; oxdock_prepare! { name: Prepared, script: { MKDIR gen WRITE gen/out.txt ... }, out_dir: "target/prebuilt_prepare" }`, assets consumed during build only (codegen, `include!`, build.rs style). Must be wired into `oxdock-doc-tests` like the embed fence.
  - `streaming pipeline` (`WITH_IO` pipes): one `oxdock` fence showing bytes routed without temp files, e.g. `WITH_IO [stdout=pipe:log] { ECHO first ECHO second }` then `WITH_IO [stdin=pipe:log] WRITE captured.txt`, plus `ASSERT_FILE` to prove it. Reuse the existing command ref example shape so `docs_conformance.rs` already covers the semantics.
- Keep llama brevity: each mode is a `###` plus one sentence plus one fence, no paragraphs about fingerprinting, sandboxing, or harness internals at the top. Those details stay lower.
- Verify: `cargo run -p docs-gen`, `cargo test -p oxdock-logic-tests --test docs_conformance`, `cargo test --doc -p oxdock-doc-tests` (or workspace doc tests) all pass with the new fences.

### Promote workspaces (ephemeral snapshot default, WORKSPACE LOCAL switch)
- Today workspaces live only in lower `fragments/workspaces.md` (5 bullets, internals heavy, contains dash punctuation). Add a concise shared workspaces note near the top of both READMEs via a shared fragment (e.g. `.oxdock/template/shared/workspaces.tmpl` or fold into shared intro), llama style:
  - Start in an ephemeral snapshot workspace by default (isolated temp dir, source tree untouched).
  - Pull inputs with `COPY` or `COPY_GIT`.
  - Switch to the local directory with `WORKSPACE LOCAL` when the script should mutate in place, plus one minimal `oxdock` fence proving the switch.
- Keep it to 3 bullets plus one fence max at the top. Trim the lower `workspaces.md` to internals (`oxdock-fs` gating, not a full sandbox, perf) and remove dash punctuation per the AGENTS.md style rule. No duplication: top fragment shared by both `output.tmpl` files.
- Keeps Docker analogy but adds Rust + dual-mode (macro vs binary) per user direction. Verbatim `.md` so no expansion concerns.

### 2b. Style guardrail (new per user request)
- Add to `AGENTS.md` under Documentation: no em dashes, en dashes, or hyphen punctuation in README prose and templates. Use periods or colons instead. Hyphens inside code, crate names, CLI flags, and versions remain allowed.
- Apply the rule to the new taglines above and audit `intro.md`, `overview.md`, `build-language.md`, and `oxdock/Cargo.toml` description for dash punctuation during implementation.
- Optional: keep second sentence short to avoid wrapping fatigue.

### 3. Root Quick start install — compact, above the fold, version-pinned
Constraint: don't push Rust example below the fold → budget max 3-4 lines between `## Quick start` and the `rust` fence.

Approach:
- a) `crates/docs-gen/src/main.rs:render_target` — add fallback: when `target.member` is `None`, load workspace version via existing `parse_workspace_version(workspace_toml)` and `env.insert_inherit_env("CRATE_VERSION", &workspace_version)` + set `provider_values` version so `{{ env:CRATE_VERSION }}` works in root templates. Small, localized change (~6 lines). No new provider needed.
- b) New shared snippet `.oxdock/template/shared/install.tmpl` (expanded):
  ```
  ```sh
  cargo install oxdock@{{ env:CRATE_VERSION }}
  ```
  ```
  Or inline one-liner to save vertical space: `Install: \`cargo install oxdock@{{ env:CRATE_VERSION }}\``.
- c) Include it in `.oxdock/template/readme/output.tmpl` immediately after `fragments/quick-start.md` and before `shared/embed-example.md`, OR prepend to `shared/embed-example.md` — prefer output.tmpl one-line insert to keep `embed-example.md` reusable by `oxdock/README.md` (which already has its own `usage.md` install).
- d) Update `.oxdock/template/readme/fragments/quick-start-cli.md:23-28`:
  - Old: `cargo install --path oxdock` + `oxdock --script Oxfile`
  - New: must match pinned style or be removed to avoid two conflicting installs. Proposal: change to `oxdock --script Oxfile` only (install already shown at top), OR `cargo install oxdock@{{ env:CRATE_VERSION }}` — but file is `.md` (verbatim), so either convert to `.tmpl` or drop the install lines here since top snippet covers it. Preferred: drop duplicate install, keep only run line.
- e) Update `oxdock/.oxdock/template/fragments/usage.md:3-7` (this target HAS `CRATE_VERSION`): `cargo install oxdock` → `cargo install oxdock@{{ env:CRATE_VERSION }}` — requires renaming `usage.md` → `usage.tmpl` (verbatim→expanded) so the placeholder expands. Check `conventional_stages`/`output.tmpl` include still picks it up (`oxdock/.oxdock/template/output.tmpl` lists `fragments/usage.md` explicitly — update that line to `usage.tmpl`).

### 4. Regenerate + verify
- `cargo run -p docs-gen` → check `git diff README.md oxdock/README.md` shows only intended lines.
- `cargo test --workspace --tests` (per AGENTS.md prefer this) — at minimum `cargo test -p docs-gen` (golden) + `cargo test -p oxdock-logic-tests --test docs_conformance` (every `oxdock` fence still passes; install `sh` fences are NOT executed as DSL, but reference-integrity check requires relative links/paths exist — version-pinned `cargo install` line is inside `sh` fence so safe).
- Confirm root Quick start: `## Quick start` → install one-liner → Rust fence all visible without scroll (measure line count: header + 1 install line + blank before fence).

## Files to modify
- `oxdock/Cargo.toml` (description)
- `.oxdock/template/readme/fragments/intro.md`
- `.oxdock/template/readme/output.tmpl`
- `.oxdock/template/shared/install.tmpl` (new, or inline alternative)
- `.oxdock/template/readme/fragments/quick-start-cli.md` (trim duplicate install)
- `oxdock/.oxdock/template/fragments/usage.md` → `usage.tmpl` + `oxdock/.oxdock/template/output.tmpl` reference update
- `crates/docs-gen/src/main.rs` (workspace-version fallback for member=None targets)

## Verification
- `cargo run -p docs-gen && git diff --stat`
- `cargo test -p docs-gen`
- `cargo test --workspace --tests` (or at least docs_conformance + doc-tests per README's "How these examples are tested")
- Manual: open `README.md:29-60` — tagline mentions Rust + CLI/macros; install shows `cargo install oxdock@0.9.0-alpha`; Rust example still immediately under Quick start.
- Manual: open `oxdock/README.md:1-10` — first line is new front-door pitch, not "Run OxDock scripts...".

## Out of scope
- Recreating/deleting `crates/oxdock/` placeholder (user already deleted it).
- Changing `oxdock-cli` / `oxdock-macros` descriptions (still accurate as variant leaves).
- Publishing to crates.io to validate `cargo install oxdock@0.9.0-alpha` resolves (alpha may need `--pre`? flag for follow-up if install fails).
